spring:
  profiles:
    active: dev
  config:
      import: optional:file:.env[.properties]


server:
  servlet:
    context-path: /api/v1

llm:
  apiKey: ${AI_API_KEY}
  modelName: gpt-4o-mini
  logRequests: true
  logResponses: true